{
  "name": "minerva-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for MINERVA",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:web": "npm run build --workspace=frontend",
    "prepare:android": "npm run build:web && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5"
  }
}
